# -> to-do
? -> un-tested

( All tasks are ordered in priority <most : up , least : down> )
(#) UPDATE 'todo.txt' BEFORE EVERY COMMIT

----------------------
. mem / xen::refernce_counter (?)

	(#) Keeps track of the number of strong and weak reference
		. destroyed when strong and weak counts are 0

----------------------
. mem / xen::observed_ref<T> (?)

	(#) wraps pointer with xen::reference_counter
		. increment strong count with every xen::observed_ref
		. destroys data when strong count hits 0
		. increment weak count with every xen::weak_ref

----------------------
. mem / xen::weak_ref<T> (?)

	(#) Fix
		. implement xen::observed_ref support
		. observe the strong count to check wether object is destroyed

----------------------
. xen::str_slice / str (?)

	(#) Non owning slice of a string

----------------------
. xen::str / str (?)

	(#) String manipulation: 
		. Concatination 
		. Trimming

----------------------
. xen::err_ctx / err (?)

	(#) xen::f_str support for err_ctx construction

----------------------
TEST:

. mem / xen:unique_ref<T>
. mem / xen::shared_ref<T>
. core / xen::safe_u64